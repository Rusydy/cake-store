version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8080:8080
    env_file:
      - .env
    # depends_on:
    #   - db

  # db:
  #   image: mysql:latest
  #   ports:
  #     - 3306:3306
  #   env_file:
  #     - .env
